{
  "name": "wolf-backoffice-backend",
  "version": "1.0.0",
  "description": "Backend for Wolf Backoffice Apps",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
    "build": "react-scripts build",
    "postinstall": "npm run build",  // Ensures Railway builds React on deploy
    "serve": "serve -s build" // Serves the React build folder
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=14"
  }
}
